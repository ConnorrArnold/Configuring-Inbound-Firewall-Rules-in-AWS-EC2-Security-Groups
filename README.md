# Configuring-Inbound-Firewall-Rules-in-AWS-EC2-Security-Groups
This project was completed in the AWS Management Console using the EC2 Security Groups interface. Inbound firewall rules were added to the launch-wizard-2 security group to control what traffic is allowed into the instance. Four rules were configured to permit common web, admin, and application traffic from any IPv4 address.
